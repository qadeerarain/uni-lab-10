# 3. Write a program that takes a string containing comma-separated values (CSV)
# as input and uses the split() function to separate the values into a list. Then, print the list.
i = input('Enter csv : ')
s = i.split(',')
print(s)